# 聚合目录与需求工作区设计

更新日期：2026-07-30

本文档是当前实现依据。此前的全局“工作区”页、组件概念、运行预设、批量启停、分阶段启动和独立工作区顶层 Tab 均已废止。

## 1. 产品模型

用户添加一个目录时，人工选择管理方式：

- 普通项目：完全沿用原 `ProjectTab`。
- 聚合目录：例如 `race`，只占一个顶层 `race` Tab。

是否为聚合目录只由用户登记结果决定。目录中存在 `.mini-ide/project.json` 不会自动改变用户选择；已有定义文件在取消聚合分类时保留，不静默删除。

界面只使用“项目”一词，不向用户展示内部历史字段名。

## 2. 聚合目录 Tab

一个聚合目录只拥有一个顶层 Tab。`race` Tab 内包含两个内部区域：

### 2.1 项目

- 展示 `race` 下人工确认的直属可运行项目。
- 每个项目一行，分别显示类型、分支/来源、状态和端口。
- 每个项目独立启动、停止，不提供启动全部、停止全部或运行预设。
- 选择项目后，在同一 `race` Tab 内展示原 `ProjectTab` 的日志、文件、Git、诊断和编译能力。
- 项目页提供 Codex 和 cc 入口；源模式打开聚合目录，需求工作区模式打开当前任务根目录。
- CLI 仍可按完整路径、目录名或稳定项目 ID 定位这些内部项目。

### 2.2 需求工作区

只展示当前聚合目录自己的需求工作区。`race` 不展示其它目录的任务，也不存在跨目录总览页。

操作包括：

- 新建需求工作区。
- 双击任务进入工作区。
- 通过页头“退出工作区”返回源目录。
- 在 Codex 中打开。
- 在 cc 中打开。
- 合并代码。
- 删除。
- 刷新。

进入需求工作区后仍停留在同一个 `race` 顶层 Tab。项目区切换为该任务的 Worktree/只读来源项目；不会创建 `race / task` 顶层 Tab。

同一聚合目录只能运行一套代码环境。切换源目录和需求工作区前，若存在运行项目，必须明确停止并确认停净。

## 3. 添加和编辑聚合目录

“文件 -> 添加目录...”流程：

1. 用户选择目录。
2. 用户选择“普通项目”或“聚合目录”。
3. 选择普通项目时，取消该路径的聚合登记，但保留已有聚合定义文件。
4. 选择聚合目录时，在 `QThread` 扫描直属候选项目并保存定义。
5. 打开一个顶层聚合目录 Tab。

聚合 Tab 内提供“编辑项目列表”，可补充、移除、重新扫描和修改项目定义。扫描、识别和保存不得阻塞主线程。

## 4. 创建需求工作区

创建时，用户填写任务名和目标，并从当前聚合目录的 Git 项目中勾选本需求会修改的项目。

规则：

- 只为勾选的 Git 项目创建 Worktree。
- 每个项目使用独立任务分支，默认格式为 `feature/<任务>-<项目ID>`。
- 未选择项目不复制、不创建 Worktree，并列入修改禁止边界。
- 非 Git 运行项目可作为源目录引用，但不能伪装成可隔离 Worktree。
- 创建前检查源路径、Git 状态、当前分支、基准提交、本地/远端同名分支和现有 Worktree。
- 创建失败时，只回滚本轮新建且仍干净的 Worktree。

任务目录位于：

```text
<聚合目录>/<workspaceDirectory>/<任务ID>/
```

至少包含：

```text
AGENTS.md
context.md
workspace.json
<所选项目 Worktree>/
```

## 5. 任务 AGENTS.md

Codex 和 cc 的工作目录必须是任务根目录。任务根目录的 `AGENTS.md` 必须写清：

- 任务名称和目标。
- 来源聚合目录。
- 当前任务根目录。
- 每个所选项目的源仓库路径。
- 基准分支和完整基准提交。
- 任务分支。
- Worktree 路径。
- 最终合并目标。
- 可修改目录。
- 未选择项目和共享来源的禁止修改边界。
- 必须继续遵守来源聚合目录及上级 `AGENTS.md`。
- 服务、日志、诊断、健康检查和编译必须走 mini-ide。
- 禁止 AI 自行合并仓库、强删分支或递归强删不确定目录。
- 只有用户通过 mini-ide 的“合并代码”明确确认后，才允许快进合并。

开发完成后，各仓库分别合并回各自记录的目标分支，不把多个仓库自动合并成一个仓库。

## 6. Codex 和 cc

需求工作区列表提供正式入口：

- “在 Codex 中打开”使用参数数组启动，`cwd` 为任务根目录。
- “在 cc 中打开”保留现有本机启动解析，`cwd` 为任务根目录。
- 子进程使用 `CREATE_NO_WINDOW`。

Codex 和 cc 是两个独立正式入口，禁止因不存在 ChatGPT 入口而删除其中任何一个。

## 7. 合并和删除

合并代码按项目执行安全快进合并：

- 工作区和源目录必须没有未提交或未跟踪文件。
- 工作区必须停留在记录的任务分支。
- 源目录必须停留在记录的基准分支。
- 基准分支与任务分支不能分叉。
- 只执行 `--ff-only`，不自动解决冲突、不创建合并提交、不改写历史。

删除前统一检查：

- 是否仍有服务运行。
- 是否存在未提交或未跟踪文件。
- 未合并分支是否已有远端备份。
- 任务根目录是否含未知文件。
- 路径是否仍在聚合目录和任务目录边界内。

删除只允许使用 `git worktree remove/prune`、普通文件删除和 `git branch -d`。禁止 `branch -D`，禁止递归删除未知目录。

## 8. 旧数据兼容

旧 `WorkspaceEntry`：

- 继续从配置读取并原样保存。
- 不在 GUI 或 CLI 展示。
- 不自动删除、不覆盖、不静默转换。
- 只允许旧迁移逻辑读取，禁止扩展成新的 Worktree 管理器。

旧 `RuntimeProfile` schema 可继续读取和往返保存，以兼容已有定义；不再提供 UI、CLI、批量启动或阶段编排。

旧会话中的 `workspace:management` token 在恢复时忽略，活动索引自动左移。需求工作区会话恢复为所属聚合目录的内部状态，不新增顶层 Tab。

## 9. CLI

工作区命令统一为：

```text
--list-workspaces
--open-workspace <target>
--close-workspace
--create-development-workspace <aggregate> <name> --projects a,b [--description text]
--workspace-review <target>
--workspace-delete-check <target>
```

聚合目录命令：

```text
--list-aggregates
--open-aggregate <target>
```

已移除：

```text
--list-runtime-profiles
--start-profile
--stop-environment
--list-development-workspaces
--open-development-workspace
```

项目启停、健康检查、日志、诊断、编译和 Git 命令继续作用于聚合 Tab 当前代码环境中的单个项目。

## 10. 工程约束

- 扫描、Git、文件创建、合并和删除必须在 `QThread`。
- UI 颜色、圆角和间距使用 `src/ui/theme.py` token。
- 不引入新依赖。
- 子进程使用 `CREATE_NO_WINDOW`。
- 服务、日志、编译和工作区生命周期走 mini-ide CLI 或现有控制器。
- 一个业务系统只占一个顶层 Tab。
- 临时任务只为会修改的仓库创建 Worktree。
- 第一版允许多任务并行编辑，但同一聚合目录只能运行一套环境。

## 11. 当前状态

- [x] 人工选择普通项目或聚合目录。
- [x] 移除全局固定“工作区”Tab。
- [x] 一个聚合目录一个顶层 Tab。
- [x] 项目逐个启动和停止。
- [x] 聚合 Tab 内嵌自己的需求工作区。
- [x] 工作区在同一 Tab 内切换 Worktree 环境。
- [x] Codex 和 cc 以任务根目录启动。
- [x] 任务 `AGENTS.md` 写入完整来源、分支、合并目标和修改边界。
- [x] 旧 `WorkspaceEntry` 保留但停止展示。
- [x] 移除运行预设 UI/CLI 和独立工作区顶层 Tab。
- [x] 冒烟测试 `48/48 imports`，颜色硬编码 `0`，`compileall` 通过。
- [ ] 重新打包根目录发布版并完成运行验证。
