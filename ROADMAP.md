# mini-ide 升级任务清单

本文档用于承接中长期改造。聚合目录最终方案以 [AGGREGATE_WORKSPACE_DESIGN.md](AGGREGATE_WORKSPACE_DESIGN.md) 为准。

当前进度：源码开发和验证已完成，根目录发布版尚未重新打包。

## 聚合目录最终方案

- [x] C.1 添加目录时人工选择“普通项目 / 聚合目录”。
  - 结果：只有人工登记路径按聚合目录打开；普通项目继续使用原 `ProjectTab`，取消聚合分类时保留已有定义文件。
- [x] C.2 一个聚合目录只占一个顶层 Tab。
  - 结果：删除全局固定“工作区”Tab和独立需求工作区顶层 Tab；`race` Tab 内含“项目”和“需求工作区”两个区域。
- [x] C.3 每个直属项目单独管理。
  - 结果：项目表逐行启动、停止，内部复用日志、文件、Git、诊断和编译；项目页 Codex/cc 自动打开当前聚合目录或任务根目录；删除运行预设、批量启停和阶段编排 UI/CLI。
- [x] C.4 每个聚合目录管理自己的需求工作区。
  - 结果：只展示本目录任务；双击进入 Worktree 环境，页头退出；工具栏只保留新建、Codex、cc、安全快进合并、删除和刷新。
- [x] C.5 Codex、cc 和任务上下文对接。
  - 结果：两个入口均保留并以任务根目录为 `cwd`；任务 `AGENTS.md` 写入来源目录、源仓库、基准分支/提交、任务分支、Worktree、合并目标、修改边界和父级规则。
- [x] C.6 旧数据和 CLI 收口。
  - 结果：旧 `WorkspaceEntry` 原样保留但 GUI/CLI 停止展示；工作区 CLI 改为操作聚合目录内部任务；移除重复开发工作区命令和运行预设命令。
- [x] C.7 源码验证。
  - 结果：冒烟测试 `49/49 imports`，Worktree 创建/合并/删除、双击进入、当前环境工具入口、聚合单 Tab、项目独立启停、CLI 匹配与旧数据保留检查通过；颜色硬编码 `0`，`compileall` 和 `git diff --check` 通过。
- [ ] C.8 重新打包和运行验证。
  - 要求：获得单独授权后，通过 mini-ide CLI 确认并停止全部运行服务，预检、退出、打包、启动，再验证新版 GUI/CLI。

废止内容：固定全局工作区页、跨目录总览、用户可见“组件”、运行预设、启动全部、停止全部、分阶段启动，以及 `race / task` 顶层 Tab。旧实现记录不再作为产品要求。

## 总目标

把 mini-ide 从“多项目启动器”升级为“可信的本机工作区控制台”：

- 状态可信：用户看到的运行、停止、外部运行、端口占用都必须有明确依据。
- 工作区清晰：`race`、`doc-ai` 这类多项目环境要能成组打开、恢复和管理。
- 退出安全：关闭 IDE 或打包前，不允许悄悄留下用户没意识到的服务。
- 日志有结论：日志窗口要能总结启动状态、失败阶段和关键错误。
- Git 服务 AI：Git 改动视图要方便复制上下文给 AI，而不只是人眼看 diff。

## 状态约定

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞或需要用户确认

## 阶段 0：已发现问题修复

- [x] Git 外层改动数与 GitViewer 不一致
  - 做法：状态栏复用同一轮 `git status` 查到的改动数量，避免短缓存造成假改动。
  - 验收：仓库干净时外层不再显示“1 处改动”，GitViewer 与状态栏一致。

- [x] 关闭项目 tab 后下次启动仍恢复
  - 做法：项目 tab 增删、切换、拖动后立即保存会话，不再只等退出时保存。
  - 验收：用户关闭某个项目 tab 后，即使 IDE 被重启或异常退出，该 tab 也不会从旧配置恢复。

- [x] 打包关闭 IDE 规则写入 AGENTS
  - 做法：明确“重新打包前关闭 mini-ide 必须先征得用户确认；用户同意后必须先停 IDE 中所有运行服务，再关闭 IDE”。
  - 验收：后续 AI 操作必须遵守此规则。

## 阶段 1：状态可信度

目标：用户看到服务状态时，能知道“它是否真的在运行、是谁启动的、依据是什么”。

- [x] 1.1 梳理当前服务状态来源
  - 怎么做：阅读 `ProjectTab`、`ServicePanel`、`ExternalProcessDetector`、`ProcessRunner`、`port_scanner`、CLI 模块状态输出。
  - 产出：列出当前状态来源、刷新频率、冲突点。
  - 验收：能解释“IDE 托管运行”“外部运行”“端口占用”“未运行”的现有判断路径。
  - 结果：
    - IDE 托管运行：来自 `ProcessRunner` 的 `idle/running/stopping` 状态，进程由当前 mini-ide 启动，日志上下文完整。
    - 多模块外部运行：来自后台端口快照 + 命令行匹配，当前 mini-ide 能识别 PID/端口，但没有启动日志上下文。
    - 端口状态来源：后台线程定期扫描 LISTEN 端口，UI 读取缓存；停止后等待端口释放时用同步 socket 探活。
    - UI 冲突点：服务面板已有 `running_external` 状态，但展示时故意和普通运行一样，用户看不出“外部运行”。
    - 日志冲突点：点击外部运行模块时，因为没有模块日志 tab，会提示“还未启动”，和左侧运行状态冲突。
    - CLI 冲突点：`--list-modules` 只输出 `running/stopped + external + pid`，没有输出端口、状态依据、日志是否接入、检测时间。
    - 关闭冲突点：项目关闭只检查当前 runner/script 是否运行，不会把外部运行模块纳入“项目正在运行”的判断。

- [x] 1.2 定义统一服务状态模型
  - 怎么做：新增或整理统一结构，至少包含：状态、来源、PID、端口、检测时间、是否有日志上下文、状态说明。
  - 状态建议：`stopped`、`starting`、`running_managed`、`running_external`、`port_occupied`、`stopping`、`unknown`。
  - 验收：UI、CLI、退出检查都能复用同一套状态语义。
  - 设计：
    - `stopped`：未发现当前模块托管进程，也未发现可信外部进程或端口占用。
    - `starting`：当前 mini-ide 正在启动该模块，日志上下文可用，端口可能尚未出现。
    - `running_managed`：当前 mini-ide 托管的进程正在运行；日志、PID、运行时长由当前实例掌握。
    - `running_external`：检测到可信外部进程属于该模块；有 PID/端口，但当前实例没有完整启动日志。
    - `port_occupied`：模块预期端口被占用，但进程归属不能可信匹配到该模块；这是风险状态，不应当显示为“运行中”。
    - `stopping`：当前 mini-ide 正在停止托管或外部进程，按钮禁用，等待最终复查。
    - `unknown`：状态检测失败、端口快照不可用或判断依据不足；必须明确显示“不确定”，不能装作已停止。
  - 统一字段：
    - `state`：上述状态之一。
    - `source`：`managed_runner`、`external_detector`、`port_probe`、`manual`、`unknown`。
    - `pid`：确认到的进程 PID；没有则为空。
    - `port`：确认到的服务端口；没有或不可信则为空。
    - `expected_port`：项目扫描出的预期端口。
    - `checked_at`：最近一次检测时间。
    - `log_attached`：当前 IDE 是否拥有该服务本次运行日志。
    - `reason`：给 UI/CLI 展示的短说明，例如“当前 IDE 启动”“外部 Java 进程”“端口被未知进程占用”“尚未检测到进程”。
    - `can_start` / `can_stop`：当前状态下按钮是否允许启动/停止。
  - 产品原则：
    - 外部运行必须显式展示，不能和 IDE 托管运行长得一样。
    - 没有日志上下文时，日志区要说“外部运行，无当前启动日志”，不能说“还未启动”。
    - 端口占用但归属不明时，不能算作服务健康运行。
    - UI、CLI、安全退出必须基于同一个状态快照，而不是各自重新解释。

- [x] 1.3 UI 展示状态来源
  - 怎么做：服务列表中区分“运行中”“外部运行”“端口占用”“状态不确定”，tooltip 或详情显示 PID/端口/检测时间。
  - 验收：外部运行服务不会再只显示成普通运行；日志缺上下文时用户能看懂原因。
  - 结果：服务面板已区分“运行”和“外部”，外部运行使用警告色；tooltip 显示端口、PID、外部运行原因和“当前 IDE 没有启动日志上下文”。

- [x] 1.4 CLI 输出增强
  - 怎么做：`--list-modules` 输出补充 source/reason/port/checked_at/log_attached 等字段，同时保持旧字段兼容。
  - 验收：AI 可通过 CLI 准确判断服务是否由 mini-ide 管理，以及能否安全停止。
  - 结果：`--list-modules` 保留旧的 `state/external/pid` 字段，同时新增 `detail_state/source/port/expected_port/log_attached/checked_at/reason`。

- [x] 1.5 状态冲突处理
  - 怎么做：当日志说未启动但端口/PID 运行时，显示“外部运行，无当前日志上下文”，而不是继续输出“还未启动”的误导提示。
  - 验收：不会再出现左侧像运行、右侧提示未启动的冲突体验。
  - 结果：点击外部运行模块时，日志区会提示该模块正在 mini-ide 之外运行，并说明当前 IDE 没有这次启动日志上下文；不再提示“还未启动”。

## 阶段 2：安全退出与打包前检查

目标：任何关闭 IDE、打包、CLI 退出路径，都要先让用户明确知道还有哪些服务在跑。

- [x] 2.1 设计退出前检查弹窗
  - 怎么做：弹窗展示所有运行中服务，包含项目、模块、状态来源、PID、端口。
  - 操作：`全部停止后退出`、`保持服务运行并退出`、`取消`。
  - 验收：用户点击关闭窗口时能看见完整服务清单和选择项。
  - 结果：主窗口关闭前集中展示仍在运行的托管/外部服务，支持“全部停止后退出”“保留外部服务退出”“取消”。

- [x] 2.2 实现 GUI 关闭流程
  - 怎么做：`closeEvent` 接入退出前检查；用户选“全部停止后退出”时走 mini-ide 现有停止逻辑。
  - 验收：GUI 退出不会默认留下服务；用户明确选择保留时才允许保留。
  - 结果：`closeEvent` 先收集全局运行服务清单，再统一决定是否停止；项目 tab 不再各自重复弹运行确认。

- [x] 2.3 实现打包前流程
  - 怎么做：AI 或开发者需要打包时，必须先请求用户确认；用户确认后先停全部运行服务，再关闭 IDE，再运行 build。
  - 验收：流程符合 AGENTS 铁律，且复查端口无监听后才打包。
  - 结果：流程规则已写入 `AGENTS.md` 和本文档；实际打包仍必须由用户单独确认后执行。

- [x] 2.4 统一 CLI quit 行为
  - 怎么做：CLI 退出命令也复用同一套停服务和保存会话逻辑。
  - 验收：GUI 退出和 CLI 退出行为一致，不出现一边停服务一边不停服务。
  - 结果：CLI quit 复用 `ProjectTab.running_service_items/stop_all_services`，返回停止服务明细。

- [x] 2.5 退出审计日志
  - 怎么做：退出前记录停了哪些服务、哪些服务保留、是否用户确认。
  - 验收：事后能从 mini-ide 日志看出关闭时发生了什么。
  - 结果：GUI 退出与 CLI quit 都记录运行服务数量、停止选择和服务清单摘要。

## 阶段 3：轻量工作区

目标：把散乱项目 tab 组织成工作区，优先解决 `race/doc-ai` 多项目混杂和恢复混乱。

- [x] 3.1 定义工作区配置结构
  - 怎么做：在配置中新增 `workspaces`，每个工作区包含名称、项目路径列表、最后激活时间、是否默认恢复。
  - 验收：兼容旧配置；没有工作区时仍按旧项目方式运行。
  - 结果：配置新增 `workspaces/active_workspace_name/startup_restore_mode`，兼容旧配置。

- [x] 3.2 自动识别候选工作区
  - 怎么做：根据共同父目录或路径模式，把 `server/webapp/nginx` 归成同一组；允许用户后续手动调整。
  - 验收：`E:\whaty\project\race\server/webapp-tenant/nginx` 可识别为一个工作区候选。
  - 结果：按最近项目的共同父目录自动生成候选工作区。

- [x] 3.3 工作区切换入口
  - 怎么做：顶部或菜单增加工作区入口，支持打开工作区、关闭当前工作区、保存当前 tab 为工作区。
  - 验收：用户可以一键打开 `race` 或 `doc-ai`，不需要逐个打开项目。
  - 结果：文件菜单新增“工作区”，支持打开工作区、保存当前 Tab 为工作区、关闭当前工作区。

- [x] 3.4 启动恢复策略
  - 怎么做：启动时恢复“上次工作区”或“上次会话”，策略可配置。
  - 验收：关闭 doc 工作区后，下次不会因为最近项目列表又自动恢复 doc。
  - 结果：配置支持 `last_session/workspace/none` 三种恢复模式；默认保留上次会话，不破坏旧行为。

- [x] 3.5 工作区级状态摘要
  - 怎么做：显示当前工作区服务运行数、Git 改动数、错误数。
  - 验收：用户能一眼看出整个工作区是否健康。
  - 结果：轻量版先通过工作区菜单显示项目数量；详细健康摘要后续可基于阶段 1 状态模型继续增强。

## 阶段 4：日志诊断

目标：日志窗口不仅展示文本，还给出启动结论和关键失败原因。

- [x] 4.1 日志事件提取
  - 怎么做：基于现有 `log_classifier`，提取启动成功、端口监听、ERROR、WARN、构建失败、数据库连接失败等事件。
  - 验收：每个服务能产出结构化诊断事件。
  - 结果：LogWidget 记录运行标签、开始/结束时间、首条错误、错误/警告数、启动完成标记和最后诊断。

- [x] 4.2 服务日志摘要栏
  - 怎么做：在日志顶部显示当前状态、启动耗时、端口、错误数、第一条 ERROR。
  - 验收：不看完整日志也能知道是否启动成功和失败大概原因。
  - 结果：复用现有计数栏和诊断条展示错误数、诊断提示；新增 `diagnosis_summary()` 汇总状态。

- [x] 4.3 常见失败原因规则
  - 怎么做：覆盖端口占用、数据库连接失败、配置缺失、依赖服务不可达、编译失败。
  - 验收：命中规则时给出明确提示和下一步建议。
  - 结果：沿用并接入现有 `log_classifier` 诊断能力，首版覆盖端口诊断等既有规则。

- [x] 4.4 外部运行日志提示
  - 怎么做：对 `running_external` 服务显示“当前 IDE 没有启动日志上下文”，并提供重新接管或停止建议。
  - 验收：不再用“还未启动”误导用户。
  - 结果：外部运行模块会显示诊断条，说明当前 IDE 没有启动日志上下文。

- [x] 4.5 诊断复制
  - 怎么做：提供“复制诊断给 AI”，包含状态、端口、关键错误、最近日志片段。
  - 验收：用户能一键把启动失败上下文交给 AI。
  - 结果：日志右键菜单新增“复制诊断给 AI”，包含状态、错误数、首条错误、诊断和关键日志。

## 阶段 5：Git 给 AI 的上下文

目标：Git 改动视图服务 AI 协作，方便复制准确、不过量的上下文。

- [x] 5.1 改动按模块/目录分组
  - 怎么做：根据路径分组显示改动文件，突出主要受影响模块。
  - 验收：大项目里能快速看出改动集中在哪些模块。
  - 结果：AI 摘要按顶层目录统计影响范围。

- [x] 5.2 改动摘要
  - 怎么做：顶部展示文件数、新增/修改/删除/冲突数量、当前分支。
  - 验收：不用展开 diff 也能知道变更规模。
  - 结果：GitViewer 可生成分支、文件数、改动类型、影响目录、numstat 摘要。

- [x] 5.3 复制 AI 上下文
  - 怎么做：按钮提供三种复制：文件列表、摘要、完整 diff。
  - 验收：复制内容包含分支、改动文件、状态和必要 diff；大 diff 会截断并提示。
  - 结果：GitViewer 新增“复制 AI 摘要”“复制文件列表”“复制完整 diff”。

- [x] 5.4 大 diff 安全截断
  - 怎么做：设置最大字符数或最大文件数，超出时只复制摘要和被截断说明。
  - 验收：不会一次复制超大内容导致 AI 上下文失控。
  - 结果：完整 diff 复制限制为 30000 字符，超出会加入截断说明。

- [x] 5.5 Git 状态刷新一致性
  - 怎么做：GitViewer、状态栏、文件树尽量复用同一轮状态或统一缓存失效策略。
  - 验收：不会再出现多个入口显示互相矛盾的 Git 状态。
  - 结果：阶段 0 已修复状态栏改动数复用同一轮 `git status` 结果；GitViewer 继续保留实时刷新。

## 执行规则

- 每次开工前，先从本文档选定一个具体任务，标成 `[~]`。
- 大任务涉及 3 个以上文件或 UI 行为变化时，先给用户确认计划。
- 每完成一个任务，必须：
  - 跑冒烟测试。
  - 更新本文档任务状态和结果说明。
  - 说明是否需要打包。
- 需要打包时，严格遵守 `AGENTS.md`：先征得用户确认；用户同意后先停所有运行服务，再关闭 mini-ide，再打包。

## 阶段 6：结构化治理

目标：降低后续 AI/人工继续开发时的误改概率，让核心业务状态有清晰边界。

- [x] 6.1 统一服务状态模型
  - 怎么做：新增纯逻辑 `ServiceState`，统一描述项目、模块、脚本的运行状态、来源、PID、端口、日志上下文和 CLI 兼容输出。
  - 验收：`ProjectTab`、CLI `--list-modules`、退出前检查不再各自手写一套状态 dict。
  - 结果：`ProjectTab.service_states()` 成为状态快照入口；`running_service_items()` 保留兼容但内部复用统一模型；CLI 模块列表直接由 `ServiceState.to_cli_module()` 生成。

- [x] 6.2 服务状态模型冒烟检查
  - 怎么做：把 `src.core.service_state` 加入 import 冒烟，并增加轻量兼容检查，验证 stopped/external 的旧 CLI 输出不变。
  - 验收：后续修改状态模型时，冒烟测试能拦住 legacy `state/external` 字段回归。
  - 结果：`scripts/_smoke.py` 已覆盖服务状态模型兼容输出和 running_items 过滤。

- [x] 6.3 抽 ProjectServiceController
  - 怎么做：把 `ProjectTab` 中服务启动、停止、重启、外部接管、等待停止逐步抽到控制器；UI 只接收信号并渲染。
  - 验收：CLI 不再直接调用 `ProjectTab` 私有启停方法，GUI/CLI 共享服务生命周期逻辑。
  - 结果：新增 `ProjectServiceController`；CLI 的 start/stop/restart/health/compile 不再直接操作 `ProjectTab` 的 runner、module_runners、外部 PID 字典。

- [x] 6.4 抽 WorkspaceManager
  - 怎么做：把工作区候选识别、保存、查找、关闭路径计算、启动恢复策略从 `MainWindow` 拆出。
  - 验收：`MainWindow` 只负责菜单和弹窗，不直接承担工作区业务判断。
  - 结果：新增 `workspace_manager` 纯逻辑模块；`MainWindow` 只负责菜单、弹窗和打开/关闭 tab 的 UI 协调。

- [x] 6.5 梳理文件树和预览大文件
  - 怎么做：在服务主线稳定后，再拆 `file_tree.py` 的文件操作和 `file_preview.py` 的文本/图片/保存逻辑。
  - 验收：文件操作、预览渲染、保存逻辑有独立边界和更小的测试面。
  - 结果：新增 `file_actions`、`tool_launchers`、`file_preview_model`、`git_context`，把文件复制/移动规则、外部工具启动命令、编码/行尾/注释/文件类型判断、Git AI 摘要和受限 diff 生成从 UI/CLI 拆出；UI 保留交互和渲染职责。冒烟测试新增文件操作安全边界、重复命名、编码、行尾、注释前缀、图片后缀和 Git 上下文规则覆盖。

## 阶段 7：AI 操作 CLI 增强

目标：让外部 AI 工具从“遥控启停”升级为“可体检、可诊断、可安全收尾”的操作入口。

- [x] 7.1 全局状态与打包前检查
  - 怎么做：新增 `--status` 返回 IDE、项目、模块、脚本、工作区和运行服务快照；新增 `--preflight-build` / `--can-quit` 在仍有运行服务时返回失败。
  - 验收：AI 打包前可以机器可读地确认是否还有服务在跑，不再靠人工截图判断。

- [x] 7.2 项目和工作区 CLI
  - 怎么做：新增 `--open`、`--close`、`--list-workspaces`、`--open-workspace`、`--close-workspace`；关闭项目/工作区时先停止服务并确认停净。
  - 验收：AI 可以打开/关闭项目和工作区；停不净时不会假装关闭成功。

- [x] 7.3 可靠启动与诊断接口
  - 怎么做：新增 `--ensure-running`；`--start/--restart` 支持 `--wait --timeout N`；新增 `--diagnose` 汇总状态、诊断摘要、错误日志和近期日志。
  - 验收：AI 不需要手动拼 start+health；排障时可一次拿到状态和关键日志上下文。

- [x] 7.4 日志与 Git 上下文接口
  - 怎么做：`--log` 支持 `--errors` 和 `--all-modules`；新增 `--git-status`、`--git-diff`、`--git-ai-context`，完整 diff 放后台线程并有 `--max-chars` 截断。
  - 验收：AI 能拿到多模块日志、错误摘要、Git 改动摘要和受控 diff，不阻塞主窗口。

- [x] 7.5 CLI 冒烟覆盖
  - 怎么做：`scripts/_smoke.py` 新增 CLI 参数解析检查，覆盖新增公开命令和旧别名。
  - 验收：后续改 CLI 时，参数解析回归会在冒烟阶段失败。

## 当前收尾

- [x] 阶段 0-5 已落地；阶段 6 已完成服务状态模型、服务控制器、工作区管理、文件树和预览核心逻辑拆分；阶段 7 已完成 AI 操作 CLI 增强。
- [x] Review 修复已完成：
  - GitViewer“复制完整 diff”改为后台生成，避免大 diff 卡住主窗口。
  - CLI quit 遇到服务停止超时会返回失败并取消退出，不再假报成功。
  - 关闭当前工作区会清空活动工作区；工作区恢复模式下会切回上次会话，避免下次又打开已关闭工作区。
  - 外部运行提示已进入“复制诊断给 AI”，包含模块、PID/端口和无当前 IDE 日志上下文说明。
  - “复制 AI 摘要”和“复制文件列表”已区分，摘要不再携带完整文件清单。
- [x] 结构化验证通过：37 个模块 import 成功，服务状态兼容检查、CLI 参数解析检查、文件操作规则检查、预览模型规则检查、Git 上下文规则检查通过，hex hardcode 扫描 0 命中。
- [x] 源码 compileall 通过。
- [x] 已打包发布并完成新版 CLI 实测：2026-06-23 已按 `AGENTS.md` 铁律先停运行服务、关闭旧版 mini-ide，再运行 `_build\scripts\build.bat`；新版启动后已验证 `--status`、项目/工作区列表、模块状态、日志、诊断、健康检查、Git 摘要、打包前检查和错误边界返回。
