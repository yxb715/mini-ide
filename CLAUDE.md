# mini-ide

本项目的完整说明已统一收敛到 [AGENTS.md](./AGENTS.md)。

为避免两份文档漂移失同步（之前出现过 CLAUDE.md 落后 AGENTS.md 十几行的情况），AI 协作时请直接读 `AGENTS.md`——里面包含：

- 项目定位与技术栈
- 目录结构（所有源码在 `_build/` 下）
- 启动与打包方式（`_build/scripts/build.bat` 一键出 exe）
- 关键设计 / 硬约束
- 项目识别 / 日志分类 / JetBrains 集成
- 行为规范 / 常见坑 / 修 bug 流程
- 沟通风格

**不要把项目说明再写一份在这里。**所有更新都进 AGENTS.md。
