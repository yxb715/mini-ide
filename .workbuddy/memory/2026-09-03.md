# 2026-09-03

## 工作区"同步并推送"固定 fetch 远端基准分支

提交 6f7072e（分支 develop，已推送 origin/develop）。

- `development_workspace_service.sync_commit_and_push_development_workspace` 移除 `fetch_remote` 参数，内部固定传 `True` 给 `_sync_one_component`。
- `aggregate_project_tab.py` 移除确认弹窗中的"先从远端拉取源分支更新"复选框与 `_pending_sync_push_fetch` 状态，日志固定记录 `fetch_remote=true`。
- 冒烟测试 `_smoke.py` 新增远端基准分支更新场景：克隆 server-upstream 写入 REMOTE_BASE.txt 推送，验证同步后本地改动、基准改动、远端改动三者都保留；冲突用例前补 `merge --ff-only origin/main`。
- 同步更新 `docs/workspaces.md` 与 `docs/logging.md` 的行为描述。

## 更新 origin 远程地址

GitHub 提示仓库已迁移，origin 由 `git@github.com:yxb715/mini-ide.git` 改为 `git@github.com:yk-yla/mini-ide.git`。`git ls-remote` 验证可达，develop 远端 HEAD 与本地一致，`git push --dry-run` 显示 up-to-date。
